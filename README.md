# 🏠 台灣實價登錄查詢工具

定期抓取政府公開的不動產實價登錄資料，提供查詢與分析功能。

## 功能特色

- 📥 自動抓取內政部實價登錄 Open Data
- 🔍 多條件篩選查詢（區域、價格、日期）
- 📊 價格趨勢分析圖表
- 📱 響應式設計，支援手機瀏覽
- ⏰ 定時自動更新（每月 2、12、22 日）

## 技術架構

- **前端**: Vue 3 + Vite + TypeScript + Chart.js
- **後端**: Node.js + Express + TypeScript
- **資料庫**: SQLite (better-sqlite3)
- **排程**: node-cron

## 快速開始

### 1. 安裝依賴

```bash
# 後端
cd backend
npm install

# 前端
cd ../frontend
npm install
```

### 2. 啟動開發環境

```bash
# 終端機 1: 啟動後端
cd backend
npm run dev

# 終端機 2: 啟動前端
cd frontend
npm run dev
```

### 3. 開啟瀏覽器

- 前端: http://localhost:3000
- API: http://localhost:3001

### 4. 抓取資料

首次使用請點擊頁面上的「更新資料」按鈕，或手動執行：

```bash
cd backend
npm run crawl
```

## API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| GET | /api/transactions | 查詢交易資料 |
| GET | /api/statistics | 取得統計資料 |
| GET | /api/trend | 取得價格趨勢 |
| POST | /api/crawl | 手動觸發資料抓取 |

### 查詢參數

`GET /api/transactions` 支援以下參數：

- `district`: 區域名稱（模糊搜尋）
- `minPrice`: 最低總價
- `maxPrice`: 最高總價
- `startDate`: 開始日期 (YYYY-MM-DD)
- `endDate`: 結束日期 (YYYY-MM-DD)
- `limit`: 回傳筆數（預設 50）
- `offset`: 分頁偏移

## 資料來源

- [內政部不動產交易實價查詢服務網](https://plvr.land.moi.gov.tw/)
- [政府資料開放平台](https://data.gov.tw/)

## 專案結構

```
real-estate-tracker/
├── backend/
│   ├── src/
│   │   ├── index.ts        # Express 伺服器
│   │   ├── database.ts     # SQLite 資料庫操作
│   │   ├── crawler.ts      # 實價登錄資料抓取
│   │   └── routes/
│   │       └── api.ts      # API 路由
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── App.vue         # 主要元件
│   │   ├── main.ts         # 入口
│   │   └── assets/
│   │       └── style.css   # 樣式
│   └── package.json
├── data/
│   └── real_estate.db      # SQLite 資料庫
└── README.md
```

## License

MIT
